- Create one HTML page with a <canvas> dom object
- Use javascript to let browser parse
- Grab the canvas object to access the context2d api
- Create a simple wrapper using class that accept size as constructor and deploy to render pixel to canvas in a scaled way
- Use browser requestAnimationFrame to loop

# NVIM

- Select multiple occurrencyt of term to replace all
- Select multiple occurrency of term, but replace ony by one with different value
- 